# Aliases 

# Shortcuts 
alias ..="cd .."
alias ...="cd ../.."
alias ....="cd ../../.."
alias .....="cd ../../../.."
alias ~="cd ~"

# Functionality 
alias sudo='sudo ' 

# Lunar Vim 
alias lvim="$HOME/.local/bin/lvim"

# Colorls 
#alias ls='colorls'
#alias 'ls -a'='colorls -a'
#alias 'ls -r'='colorls -r'
#alias lc='colorls -lA --sd'

